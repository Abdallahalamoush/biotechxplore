Options -Indexes

<IfModule mod_rewrite.c>
RewriteEngine On

# Do not use RewriteBase / if you are in a subfolder!
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>